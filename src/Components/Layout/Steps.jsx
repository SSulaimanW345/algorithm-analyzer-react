const Steps = (props) => {
  console.log(props.type);
  console.log(props.arr);
  return (
    <div className="p-2 col-md-5 ">
      <h1>Steps for which algorithm</h1>
    </div>
  );
};

export default Steps;
